# Copy these values into the backend host's environment. Never commit real
# passwords, database URLs, AWS keys, or SMTP credentials.
DATABASE_PATH=leaderboard.db
DATABASE_URL=
CORS_ORIGINS=http://localhost:5173,http://localhost:8443
REQUIRE_EMAIL_VERIFICATION=false
SHOW_DEV_AUTH_TOKENS=false
