# Local development defaults to SQLite when DATABASE_URL is empty.
DATABASE_URL=
PORT=5000

# Enable these only for a hosted deployment with a real mail provider.
REQUIRE_EMAIL_VERIFICATION=true
SHOW_DEV_AUTH_TOKENS=false
